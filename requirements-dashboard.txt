# Dependencias OPCIONALES del dashboard (dashboard_server.py).
#
# Sin esto el dashboard funciona igual, con http.server de la libreria
# estandar y polling incremental. Con esto los logs llegan por WebSocket
# (empuje del servidor: aparecen al instante y sin una request por segundo
# por cada proceso vivo), que es lo que se nota cuando corres mapeo ROS2,
# que escupe muchisimas lineas.
#
#   pip install -r requirements-dashboard.txt
#
# Nota: el venv del SDK ya trae fastapi (el SDK es FastAPI + hypercorn), asi
# que muchas veces alcanza con levantar el dashboard con ese python:
#   ./rover_launch.sh dashboard        # lo elige solo si puede
pip