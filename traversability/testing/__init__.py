"""Offline testing/optimization tools for rover_traversability's PolicyConfig.

Depends only on the existing `rover_traversability` package (installed
alongside this one, e.g. via `pip install -e ./traversability`). Nothing here
duplicates policy logic — see README.md for the architecture.
"""
