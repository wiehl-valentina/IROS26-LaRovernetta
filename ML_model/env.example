# Copiá este archivo a .env (sin el .example) y completá la ruta real.
# .env ya está en .gitignore, no se sube al repo.

# Ruta a la raiz del repo de navegacion (el que tiene adentro genie/ y
# traversability/). Se usa desde config_paths.py para poder hacer
# `import sam2` / `from rover_traversability import ...` sin instalar nada.
NAV_REPO_PATH=C:\Users\TuUsuario\IROS26-LaRovernetta
