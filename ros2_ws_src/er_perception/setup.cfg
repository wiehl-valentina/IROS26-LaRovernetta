[develop]
script_dir=$base/lib/er_perception

[install]
install_scripts=$base/lib/er_perception

[options]
install_requires=
    setuptools
packages=find:

[options.entry_points]
console_scripts=
    traversability_node = er_perception.traversability_node:main
